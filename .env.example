AUTOCLIP_CONFIG=/app/config.yaml
AUTOCLIP_TTS_PROVIDER=google-gemini
# Native Google Gemini TTS uses Application Default Credentials from
# `gcloud auth application-default login`; never put a credential file here.
AUTOCLIP_GOOGLE_CLOUD_PROJECT=
AUTOCLIP_GOOGLE_TTS_MODEL=gemini-2.5-flash-tts
AUTOCLIP_GOOGLE_TTS_VOICE=Fenrir
AUTOCLIP_GOOGLE_TTS_PARALLELISM=6
AUTOCLIP_TTS_SILENCE_TRIM_ENABLED=false
AUTOCLIP_TTS_SILENCE_THRESHOLD_DB=-45
AUTOCLIP_TTS_MINIMUM_SILENCE_SECONDS=0.10
AUTOCLIP_TTS_RETAINED_EDGE_SECONDS=0.06
AUTOCLIP_FFMPEG_SCENE_PARALLELISM=2
AUTOCLIP_WORKSPACE=/app/workspaces
# AI Studio key for the automatic /ai ZIP builder. Keep the real value only in
# ignored .env; it is never bundled into a video package.
GEMINI_API_KEY=
# Editable 12-zodiac weekly drafts with server-side web search.
DEEPSEEK_API_KEY=
AUTOCLIP_DEEPSEEK_MODEL=deepseek-v4-flash
AUTOCLIP_DEEPSEEK_BASE_URL=https://api.deepseek.com
AUTOCLIP_AI_PROVIDER=antigravity
AUTOCLIP_AI_IMAGE_PROVIDER=gemini
AUTOCLIP_GEMINI_TEXT_MODEL=gemini-3.6-flash
AUTOCLIP_GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
YOUTUBE_CLIENT_ID=your-google-oauth-client-id
YOUTUBE_CLIENT_SECRET=your-google-oauth-client-secret
YOUTUBE_REDIRECT_URI=http://localhost:9999/api/youtube/callback
YOUTUBE_API_KEY=

# Native RunPod Wan 2.2 through a local SSH tunnel.
# AutoClip itself only calls this fixed local address; do not put Pod IPs,
# SSH endpoints, or private-key paths in this file.
AUTOCLIP_WAN_ENABLED=false
AUTOCLIP_WAN_COMFY_URL=http://127.0.0.1:18188
AUTOCLIP_WAN_REQUEST_TIMEOUT_SECONDS=120
AUTOCLIP_WAN_GENERATION_TIMEOUT_SECONDS=900
AUTOCLIP_WAN_POLL_INTERVAL_SECONDS=2

# Optional GPU Thai narration through the same RunPod SSH connector.
AUTOCLIP_RUNPOD_F5_ENABLED=false

# RunPod monitor only. Keep real values in ignored .env, never in Git.
RUNPOD_SSH_HOST=
RUNPOD_SSH_PORT=22
RUNPOD_SSH_USER=root
RUNPOD_SSH_KEY_PATH=~/.ssh/id_ed25519_runpod

# YouTube Podcast Generator Settings
PODCAST_TTS_CHUNK_MAX_BYTES=2800
PODCAST_TTS_CONCURRENCY=3
PODCAST_TTS_MAX_RETRIES=5
PODCAST_DEFAULT_VOICE=Iapetus
PODCAST_DEFAULT_SPEED=0.90
PODCAST_DEFAULT_BGM_TRACK=mamase-podcast-bg.mp3
PODCAST_DEFAULT_BGM_VOLUME=0.08
# Mamase Reel Hook Gate (runs after planning and before image generation)
AUTOCLIP_REEL_HOOK_GATE_ENABLED=true
AUTOCLIP_REEL_HOOK_MAX_CHARACTERS=120
