# Let's write the complete, updated HTML templates to scripts/build_thai_java_zone_package.py
import re
from pathlib import Path

script_path = Path("scripts/build_thai_java_zone_package.py")
print("Script exists:", script_path.exists())
